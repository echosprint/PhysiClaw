name: pay
description: confirm payment on the buy sheet (免密支付)
enabled: false
steps:
  - name: pay-button
    tool: tap
    with: {bbox: [0.39, 0.915, 0.69, 0.958]}
  - name: settle
    tool: wait
    with: {seconds: 4}
